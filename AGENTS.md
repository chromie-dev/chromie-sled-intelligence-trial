# Working agreement

Run tests before every handoff. Preserve raw source IDs and URLs through every transform. Never silently invent missing values. Prefer a small correct pipeline over broad unsupported scraping. Record assumptions in `DECISIONS.md`.

